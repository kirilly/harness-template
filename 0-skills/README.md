# Skills

Skills are reusable knowledge documents that Claude loads when relevant.

Place skill files here as `<skill-name>/<skill-name>.md`.

Skills are referenced in `.claude/settings.json` under the `skills` section.
